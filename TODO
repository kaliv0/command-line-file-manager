- test organize plain,
    recursively,
    recursively-flatten,
    archive(?),
    with/without 'hidden'
- add dir_diff as separate feature
    -> re-use for checking previous task?
- expand readme / add docs?

- refactor scanner save logs -> unify with organizer (?!)